const nombre = "Ang";
const nota  = 4.0;
const promedio =
    nota >= 3.0 ? "Aprobado":
    nota < 3.0 ? "Reprobado":
    "";

console.log(`Hola ${nombre}, tu nota es ${nota}: ${promedio}`);


